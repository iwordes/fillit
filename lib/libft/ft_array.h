#ifndef FT_ARRAY_H
# define FT_ARRAY_H

size_t	ft_arrfind_char(char *arr, char find, size_t limit);
size_t	ft_arrfind_str(char **arr, char *find, size_t limit);
size_t	ft_arrfind_int(int *arr, int find, size_t limit);

#endif
